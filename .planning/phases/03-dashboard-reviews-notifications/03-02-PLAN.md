---
phase: 03-dashboard-reviews-notifications
plan: 02
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - extension/popup.html
  - extension/popup.css
  - extension/popup.js
autonomous: true
requirements:
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Review queue lists each due problem with title, difficulty, LeetCode link, and 4 rating buttons"
    - "Clicking Open on LeetCode opens a new tab to the problem page"
    - "After rating a card, it animates out and the due count updates"
    - "Empty review queue shows All caught up message"
    - "Settings tab saves OpenRouter API key and notification preferences"
    - "Settings persist across popup close/reopen without page reload"
  artifacts:
    - path: "extension/popup.html"
      provides: "Reviews tab and Settings tab markup"
      contains: "notif-enabled"
    - path: "extension/popup.css"
      provides: "Review card styling, rating buttons, settings form, card removal animation"
      contains: "review-card"
    - path: "extension/popup.js"
      provides: "Review queue rendering, rating handler, settings save/load"
      contains: "RATE_REVIEW"
  key_links:
    - from: "extension/popup.js"
      to: "extension/background.js"
      via: "chrome.runtime.sendMessage GET_DUE_TODAY and RATE_REVIEW"
      pattern: "sendMessage.*RATE_REVIEW"
    - from: "extension/popup.js"
      to: "chrome.tabs.create"
      via: "Opens LeetCode problem in new tab"
      pattern: "chrome\\.tabs\\.create"
    - from: "extension/popup.js"
      to: "chrome.storage.local"
      via: "Settings save/load with merge pattern"
      pattern: "chrome\\.storage\\.local\\.(get|set)"
---

<objective>
Build the Reviews tab with inline rating workflow and the Settings tab with API key and notification preferences.

Purpose: Delivers the core review interaction (rate cards to update FSRS scheduling) and settings persistence for notifications and future AI features.
Output: Fully functional Reviews and Settings tabs in the popup.
</objective>

<execution_context>
@/Users/sumi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sumi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-reviews-notifications/03-CONTEXT.md
@.planning/phases/03-dashboard-reviews-notifications/03-RESEARCH.md
@.planning/phases/03-dashboard-reviews-notifications/03-01-SUMMARY.md
@.planning/phases/03-dashboard-reviews-notifications/03-03-SUMMARY.md

<interfaces>
<!-- Background message handlers used by this plan (already exist in background.js) -->
<!-- Plan 03-03 enhances GET_DUE_TODAY to join title/difficulty from submissions store -->
GET_DUE_TODAY response: { cards: Array<{ titleSlug, title, difficulty, due, state, ... }> }
RATE_REVIEW payload: { titleSlug: string, rating: 'Again'|'Hard'|'Good'|'Easy' }
RATE_REVIEW response: { ok: true } | { error: string }

<!-- Settings object shape in chrome.storage.local (key: 'settings') -->
Existing fields: { captureEnabled: true }
New fields to merge: { openRouterApiKey: string, notificationsEnabled: boolean, notificationTime: string }
CRITICAL: Must spread existing settings before writing — do NOT overwrite captureEnabled.

<!-- From Plan 01: popup.html has tab-reviews and tab-settings divs, popup.js has tab switching -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reviews tab with inline rating and card removal animation</name>
  <files>extension/popup.html, extension/popup.css, extension/popup.js</files>
  <action>
1. **popup.html** — Add Reviews tab content inside `#tab-reviews`:
   - Review count header (e.g., "3 reviews due")
   - Review list container div (`#review-list`)
   - Empty state div (`#review-empty`) with "All caught up!" text, hidden by default

2. **popup.css** — Add review card styles:
   - `.review-card`: card with padding, border, margin-bottom, border-radius, position relative, overflow hidden, transition for opacity and max-height (0.3s ease)
   - `.review-card.removing`: opacity 0, max-height 0, margin 0, padding 0 (for exit animation)
   - Problem title: bold, clickable link style
   - Difficulty badge: inline colored badge (green #00b8a3 Easy, orange #ffc01e Medium, red #ff375f Hard) — same as dashboard
   - Rating buttons row: 4 buttons (Again/Hard/Good/Easy) with distinct colors — Again red, Hard orange, Good blue, Easy green. Compact sizing to fit in card.
   - `.review-card a`: text-decoration none, color #0066cc, hover underline

3. **popup.js** — Add review loading and rating logic:
   - `loadReviews()`: call `chrome.runtime.sendMessage({ type: 'GET_DUE_TODAY' })`, render cards or empty state
   - `renderReviewQueue(cards)`: for each card, create a `.review-card` element containing:
     - Problem title as a clickable link (uses `card.title` from the enhanced GET_DUE_TODAY response, which joins title/difficulty from the submissions store — this is handled by Plan 03-03 in background.js)
     - Link onclick: `chrome.tabs.create({ url: 'https://leetcode.com/problems/' + card.titleSlug + '/' })`
     - Difficulty badge using `card.difficulty` (one of 'Easy', 'Medium', 'Hard'). If difficulty is missing, omit the badge gracefully.
     - 4 rating buttons, each with data-rating attribute. On click: call `chrome.runtime.sendMessage({ type: 'RATE_REVIEW', payload: { titleSlug, rating } })`, then animate card removal.
   - `removeCard(cardEl, onComplete)`: set `removing` class, listen for `transitionend` with `{ once: true }`, then `cardEl.remove()` and call onComplete. After removal, update the review count header. If no cards remain, show empty state.
   - Update review count in tab and refresh dashboard stats after a rating (since totalReviews changes)
   - Call `loadReviews()` when Reviews tab becomes active (on tab switch)
  </action>
  <verify>
    <automated>cd /Users/sumi/leetreminder && node -e "const js = require('fs').readFileSync('extension/popup.js','utf8'); const checks = [js.includes('RATE_REVIEW'), js.includes('chrome.tabs.create'), js.includes('transitionend'), js.includes('loadReviews')]; console.log('reviews:', checks.every(Boolean) ? 'PASS' : 'FAIL', checks);" && node -e "const css = require('fs').readFileSync('extension/popup.css','utf8'); console.log('animation:', css.includes('.removing') || css.includes('removing') ? 'PASS' : 'FAIL');"</automated>
  </verify>
  <done>
    - Reviews tab shows due cards with title, difficulty badge, link, and 4 rating buttons
    - Clicking a problem link opens LeetCode in a new tab
    - Rating a card sends RATE_REVIEW message and animates card out
    - Empty queue shows "All caught up!" message
    - Review count header updates after each rating
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings tab with API key and notification preferences</name>
  <files>extension/popup.html, extension/popup.css, extension/popup.js</files>
  <action>
1. **popup.html** — Add Settings tab content inside `#tab-settings`:
   - Section header "Settings"
   - Form group: "OpenRouter API Key" label + password input (`#api-key`) with placeholder "sk-or-..." (for v2 AI feedback — store now, use later)
   - Form group: "Notifications" label + checkbox (`#notif-enabled`) labeled "Enable review reminders"
   - Form group: "Reminder Time" label + time input (`#notif-time`) with default 09:00
   - Save button (`#save-settings`) — or auto-save on change/blur for better UX
   - Status indicator: "Settings saved" brief flash after save

2. **popup.css** — Settings form styles:
   - Form groups with vertical layout, margin-bottom between groups
   - Input fields: full width, padding, border, border-radius
   - Checkbox + label inline
   - Time input styled consistently with text input
   - Save status: green text, fade-out after 2 seconds
   - Overall settings panel padding

3. **popup.js** — Settings save/load logic:
   - `loadSettings()`: read from `chrome.storage.local.get('settings')`, populate form fields. Default notificationsEnabled to true, notificationTime to '09:00', openRouterApiKey to empty.
   - `saveSettings()`: CRITICAL — read existing settings first with `chrome.storage.local.get('settings')`, spread existing values, then overlay new values. This preserves `captureEnabled` and any other existing fields. Write back with `chrome.storage.local.set({ settings: merged })`.
   - Wire save: add event listener to save button (or auto-save on input change/blur with debounce)
   - Show "Settings saved" confirmation briefly after save
   - Call `loadSettings()` when Settings tab becomes active (on tab switch)
  </action>
  <verify>
    <automated>cd /Users/sumi/leetreminder && node -e "const html = require('fs').readFileSync('extension/popup.html','utf8'); const checks = [html.includes('api-key'), html.includes('notif-enabled'), html.includes('notif-time')]; console.log('settings html:', checks.every(Boolean) ? 'PASS' : 'FAIL');" && node -e "const js = require('fs').readFileSync('extension/popup.js','utf8'); const hasMerge = js.includes('...') || js.includes('Object.assign'); const hasLoad = js.includes('loadSettings'); console.log('settings js:', hasMerge && hasLoad ? 'PASS' : 'FAIL');"</automated>
  </verify>
  <done>
    - Settings tab shows API key input, notification toggle, and reminder time picker
    - Saving settings preserves existing captureEnabled field (merge, not overwrite)
    - Settings persist across popup close/reopen
    - Save confirmation shown briefly after saving
  </done>
</task>

</tasks>

<verification>
- Reload extension, open popup, switch to Reviews tab
- If due cards exist, they show with title, difficulty badge, link, and rating buttons
- Click "Open on LeetCode" — new tab opens to the problem
- Rate a card — it animates out, count updates
- When all rated, "All caught up!" appears
- Switch to Settings tab — enter API key, toggle notifications, set time, save
- Close and reopen popup — settings persist
</verification>

<success_criteria>
- Reviews tab renders due cards with working rating buttons
- Card removal animation plays on rating
- LeetCode links open in new tabs
- Settings save and load correctly with merge pattern
- No captureEnabled data loss after saving settings
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-reviews-notifications/03-02-SUMMARY.md`
</output>
