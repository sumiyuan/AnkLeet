---
phase: 03-dashboard-reviews-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/manifest.json
  - extension/popup.html
  - extension/popup.css
  - extension/popup.js
  - extension/background.js
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "Opening the extension popup shows a tabbed UI with Dashboard, Reviews, and Settings tabs"
    - "Dashboard tab displays today's problems attempted with attempt counts"
    - "Stats bar shows retention rate, review count, and streak"
  artifacts:
    - path: "extension/manifest.json"
      provides: "Popup declaration and new permissions"
      contains: "default_popup"
    - path: "extension/popup.html"
      provides: "Popup shell with tab navigation"
      min_lines: 30
    - path: "extension/popup.css"
      provides: "Tab layout, stats bar, activity list styling"
      min_lines: 40
    - path: "extension/popup.js"
      provides: "Tab switching, data fetch, dashboard rendering"
      min_lines: 50
    - path: "extension/background.js"
      provides: "GET_TODAY_SUBMISSIONS message handler"
      contains: "GET_TODAY_SUBMISSIONS"
  key_links:
    - from: "extension/popup.js"
      to: "extension/background.js"
      via: "chrome.runtime.sendMessage GET_STATS and GET_TODAY_SUBMISSIONS"
      pattern: "sendMessage.*GET_TODAY_SUBMISSIONS"
    - from: "extension/manifest.json"
      to: "extension/popup.html"
      via: "action.default_popup"
      pattern: "default_popup.*popup.html"
---

<objective>
Wire manifest for popup and new permissions, build the popup shell with tab navigation, add GET_TODAY_SUBMISSIONS handler to background.js, and render the Dashboard tab with stats bar and today's activity list.

Purpose: Establishes the popup UI foundation that all other plans build on, and delivers the first user-visible feature (daily activity dashboard).
Output: Working popup with Dashboard tab showing stats and today's activity; Reviews and Settings tabs present but empty.
</objective>

<execution_context>
@/Users/sumi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sumi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-reviews-notifications/03-CONTEXT.md
@.planning/phases/03-dashboard-reviews-notifications/03-RESEARCH.md

<interfaces>
<!-- Existing background.js message handlers the popup will call -->
From extension/background.js:

GET_STATS response: { totalReviews: number, retentionRate: number, streak: number }
GET_DUE_TODAY response: { cards: Array<{ titleSlug, due, stability, difficulty, ... }> }
RATE_REVIEW payload: { titleSlug: string, rating: 'Again'|'Hard'|'Good'|'Easy' }

<!-- submissions store schema (for GET_TODAY_SUBMISSIONS) -->
submissions record: {
  id (autoIncrement), submissionId, titleSlug, title, difficulty, topicTags,
  url, code, lang, langDisplay, statusDisplay, capturedAt (Date.now() integer ms)
}
Indexes: submissionId (unique), titleSlug, capturedAt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire manifest, create popup shell, and add GET_TODAY_SUBMISSIONS handler</name>
  <files>extension/manifest.json, extension/popup.html, extension/popup.css, extension/popup.js, extension/background.js</files>
  <action>
1. **manifest.json** — Add `action` key with `default_popup: "popup.html"` and `default_icon` referencing existing icons. Add `"alarms"` and `"notifications"` to the `permissions` array (needed by Plan 03, but add now to avoid a second manifest edit).

2. **popup.html** — Create the popup shell:
   - `<!DOCTYPE html>` with charset utf-8
   - `<link rel="stylesheet" href="popup.css">`
   - Tab navigation bar: three buttons with `data-tab` attributes — "Dashboard", "Reviews", gear icon (&#9881;) for Settings
   - Three `<div class="tab-panel">` containers: `tab-dashboard` (active by default), `tab-reviews`, `tab-settings`
   - Dashboard panel contains: stats bar div (3 stat items: retention %, reviews count, streak days), activity list container
   - Reviews panel: placeholder div (populated by Plan 02)
   - Settings panel: placeholder div (populated by Plan 02)
   - `<script src="popup.js"></script>` at end of body (NO inline scripts — MV3 CSP)
   - Fixed dimensions: body width 380px, min-height 500px

3. **popup.css** — Style the popup:
   - Body: margin 0, font-family system-ui/-apple-system, background #f8f9fa, width 380px
   - Tab bar: flex row, border-bottom, tab buttons with active state indicator (bottom border or background)
   - `.tab-panel { display: none; overflow-y: auto; }` / `.tab-panel.active { display: block; }`
   - Stats bar: flex row with 3 equal columns, each showing a number and label, light card background
   - Activity list: each item shows problem title, difficulty badge (color-coded: green Easy, orange Medium, red Hard), attempt count
   - Empty state: subtle "No activity today" text when list is empty

4. **popup.js** — Implement tab switching and Dashboard data loading:
   - Tab switching: addEventListener on each `.tab-btn`, toggle `active` class on buttons and panels
   - `loadDashboard()`: call `chrome.runtime.sendMessage({ type: 'GET_STATS' })` and `chrome.runtime.sendMessage({ type: 'GET_TODAY_SUBMISSIONS' })` in parallel via Promise.all
   - `renderStats(stats)`: populate stats bar with `stats.retentionRate`, `stats.totalReviews`, `stats.streak`
   - `renderTodayActivity(submissions)`: aggregate by titleSlug (counting attempts per problem), render each as a list item with title, difficulty badge, and attempt count. Use `aggregateTodayActivity()` helper that groups by titleSlug and sums attempts.
   - Call `loadDashboard()` on DOMContentLoaded
   - Also load data when switching TO the dashboard or reviews tab (refresh on tab switch)

5. **background.js** — Add `GET_TODAY_SUBMISSIONS` message handler:
   - Add new `if (message.type === 'GET_TODAY_SUBMISSIONS')` block in the existing `onMessage.addListener` callback, following the same async pattern as GET_DUE_TODAY
   - Implement `getTodaySubmissions(database)` function: query submissions store using `capturedAt` index with `IDBKeyRange.bound(start.getTime(), end.getTime())` where start is today 00:00:00.000 and end is today 23:59:59.999. CRITICAL: capturedAt is stored as Date.now() (integer ms), NOT ISO string.
   - Place the new handler BEFORE the `return false` at the end of the listener

IMPORTANT: No inline scripts in popup.html. All event listeners via addEventListener in popup.js. Settings object in chrome.storage.local must not be overwritten (merge pattern).
  </action>
  <verify>
    <automated>cd /Users/sumi/leetreminder && node -e "const m = JSON.parse(require('fs').readFileSync('extension/manifest.json','utf8')); const ok = m.action?.default_popup === 'popup.html' && m.permissions.includes('alarms') && m.permissions.includes('notifications'); console.log('manifest:', ok ? 'PASS' : 'FAIL');" && node -e "const h = require('fs').readFileSync('extension/popup.html','utf8'); const noInline = !/<script[^>]*>(?!<\/script>)[^<]+/i.test(h); const hasTabs = h.includes('tab-dashboard') && h.includes('tab-reviews') && h.includes('tab-settings'); console.log('html:', noInline && hasTabs ? 'PASS' : 'FAIL');" && node -e "const js = require('fs').readFileSync('extension/popup.js','utf8'); console.log('popup.js:', js.includes('GET_TODAY_SUBMISSIONS') && js.includes('GET_STATS') ? 'PASS' : 'FAIL');" && node -e "const bg = require('fs').readFileSync('extension/background.js','utf8'); console.log('bg handler:', bg.includes('GET_TODAY_SUBMISSIONS') && bg.includes('IDBKeyRange.bound') ? 'PASS' : 'FAIL');"</automated>
  </verify>
  <done>
    - Extension popup opens when clicking the extension icon
    - Three tabs visible: Dashboard, Reviews, Settings
    - Dashboard tab shows stats bar (retention %, review count, streak) with data from background.js
    - Dashboard tab shows today's activity list aggregated by problem with attempt counts
    - GET_TODAY_SUBMISSIONS handler queries submissions by capturedAt using numeric IDBKeyRange
    - No inline scripts in popup.html
  </done>
</task>

</tasks>

<verification>
- Load unpacked extension in chrome://extensions (reload if already loaded)
- Click extension icon — popup should open with ~380px width
- Dashboard tab should be active by default showing stats bar and activity list
- Tab switching between Dashboard/Reviews/Settings should work
- If problems were solved today, they appear in the activity list with attempt counts
</verification>

<success_criteria>
- Popup opens with tabbed navigation
- Dashboard tab renders stats and today's activity from live IndexedDB data
- Tab switching works for all three tabs
- No console errors related to CSP or undefined handlers
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-reviews-notifications/03-01-SUMMARY.md`
</output>
